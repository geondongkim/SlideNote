rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 로그인한 사용자만 본인 폴더에 업로드/다운로드 가능
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
